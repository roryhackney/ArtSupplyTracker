/**
 * Observer interface used for Observers of Subjects
 */
public interface ObserverInterface {
    /**
     * updates the Observer that Subject data has changed
     */
    void update();
}
